<%= form_for [@post, @post.comments.build] do |f| %>
    <%= f.hidden_field :user_id, value: @post.user.id %>
    <p>
    <%= f.label :body %>
    <%= f.text_area :body %>
    </p>
    <p>
    <%= f.submit "Submit" %>
    </p>
<% end %>
